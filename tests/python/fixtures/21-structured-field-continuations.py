def load_value(path):
    """Load a value from a path.

    :param path: Path to inspect.  The path may be relative
        to the current working directory.
    :returns: The loaded value.  The value may retain
        source-specific metadata.
    :raises ValueError: The path is invalid.  The message may include
        source-specific context.
    """
    return path
