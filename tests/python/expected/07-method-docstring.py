class Repository:
    """Represent a repository."""

    def load(self, path: str) -> str:
        """Load a repository path.

        @param path Repository path to load.
        @return The loaded path.
        """
        return path
